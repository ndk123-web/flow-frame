# Event-Driven Architecture

Decouples services using an asynchronous event queue. Processes requests as event messages.

## Key Benefits
- **Asynchronous Ingestion**: Ingestion servers accept requests and queue them instantly, reducing response times.
- **Worker Scaling**: Consumers pull tasks and process them at their own pace.
