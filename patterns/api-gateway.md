# API Gateway Pattern

Exposes a single endpoint interface routing calls to downstream microservices.

## Key Benefits
- **Unified Interface**: Clients talk to one gateway.
- **Prefix Isolation**: Internal service changes do not affect external API contract names.