# Monolithic Architecture

Consolidates all business logic, routing, caching, and database lookups into a single server block.

## Key Benefits
- **Simple Deployment**: Single package deployment.
- **Low Latency**: Sockets/in-memory data calls replace network queries.