# Postgres Database Component Spec

PostgreSQL is a persistent relational database storing system tables and user records.

## Core Capabilities
- **Persistent Storage**: Writes values permanently to tables.
- **Concurrency Locks**: Processes queries sequentially or concurrently.
- **Transaction Logs**: Tracks transaction metrics and write locks.
